<Window x:Class="WardrobeWizard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WardrobeWizard"
        xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern" 
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:TitleBar.Height="36"
        mc:Ignorable="d"
        Title="hello" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Left navigation bar takes 1/5 of the width -->
            <ColumnDefinition Width="1*" />
            <!-- Main content takes 4/5 of the width -->
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <ui:NavigationView x:Name="MyNavigationView" PaneDisplayMode="Left" IsBackButtonVisible="Collapsed">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="主页" Icon="H" Tag="HomePage"/>
                <ui:NavigationViewItem Content="穿搭" Icon="C" Tag="FashionPage"/>
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>

        <ContentControl x:Name="MainContent" Grid.Column="1" />

    </Grid>
    
</Window>
